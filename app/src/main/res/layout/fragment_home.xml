<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    style="@style/Theme.GovJobKG.White"
    android:layout_height="match_parent">

    <include
        layout="@layout/layout_toolbar"
        android:id="@+id/toolbar"
        />

    <ImageView
        android:layout_width="150dp"
        android:layout_height="150dp"
        android:src="@drawable/logo"
        android:id="@+id/ivLogo"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@id/toolbar"
        android:layout_marginTop="30dp"
        android:scaleType="fitCenter"
        />

    <!-- Informative text with better styling -->
    <TextView
        android:id="@+id/tvInformativeText"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Gov Job — это платформа, где  могут легко добавлять вакансии, чтобы соискатели из Кыргызстана могли находить работу."
        android:textSize="18sp"
        android:textColor="@color/black"
        android:textAlignment="center"
        android:padding="16dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@id/ivLogo"
        android:layout_marginTop="20dp"
        app:layout_constraintHorizontal_bias="0.5"
        />

    <Button
        android:id="@+id/btnAdd"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginHorizontal="14dp"
        android:text="Добавить вакансию"
        android:textColor="@android:color/white"
        android:textSize="16sp"
        android:elevation="4dp"
        app:layout_constraintTop_toBottomOf="@id/tvInformativeText"
        android:layout_marginTop="40dp"
        />

</androidx.constraintlayout.widget.ConstraintLayout>
